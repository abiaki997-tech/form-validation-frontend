{
  "name":"calibre",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:prod":"NODE_ENV = production webpack -p -progess --colors --ball --optimize-minimize",
    "postinstall": "npm run build:prod"
  },
  "engines": {
    "node": "7.8.0",
    "npm": "4.2.0"
  }

}